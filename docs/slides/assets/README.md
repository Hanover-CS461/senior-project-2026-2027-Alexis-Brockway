# Slide assets

Put images used by the Marp slides in this folder (e.g. `assets/diagram.png`)
and reference them from `docs/slides/*.md` as `assets/diagram.png`.

This directory is copied into the built site at `_site/slides/assets/` by the
GitHub Actions workflow.